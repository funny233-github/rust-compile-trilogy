#import "../lib.typ": *
= 图着色寄存器分配
#labnote[ 第六站：NP 完全问题的优雅近似 ]

前一章讲了寄存器分配的基本概念和线性扫描。这一章深入图着色法——寄存器分配理论中更优雅、也更复杂的方法。

== 为什么需要比线性扫描更好的方法

线性扫描快，但有个盲区：它只看活跃区间的起点和终点，不关心变量之间*谁和谁同时存活*。

```
; 例子：两个不重叠但线性扫描会搞错的情况
r0 = ...       ; r0 定义，活跃到第 4 行
r1 = ...       ; r1 定义，活跃到第 3 行
... use r1 ... ; r1 最后使用
... use r0 ... ; r0 最后使用
```

线性扫描按定义顺序处理：r0 拿走 reg0，r1 拿走 reg1。但如果 r2 稍后出现且需要寄存器，线性扫描可能做出次优的溢出决策。

图着色法*全局地*看待所有变量及其冲突关系，有机会得到更优的分配。

== 干涉图 (Interference Graph)

#definition[
  *干涉图*是一个无向图：
  - *节点* = 每个虚拟寄存器
  - *边* = 两个虚拟寄存器的活跃范围有重叠（它们"干涉"）

  如果两个节点之间没有边，它们可以共享同一个物理寄存器。
]

构建干涉图：

```
; 代码               ; 活跃范围
r0 = a + b            r0: [1, 2]
r1 = r0 * c           r1: [2, 5]
r2 = d - e            r2: [3, 4]
r3 = r1 + r2          r3: [5, 5]
```

活跃范围重叠关系：
- r0 和 r1 同时存活于第 2 行 → 干涉
- r1 和 r2 同时存活于第 3-4 行 → 干涉
- r0 和 r2: r0 在第 3 行已死亡 → 不干涉
- r3 只活在第 5 行，和谁都不干涉

干涉图：

```
r0 —— r1 —— r2   (r0 和 r2 之间无边！)
         r3 (孤立点)
```

#concept[
  干涉图把"谁能和谁共享寄存器"变成了图的可视化问题。

  没有边 = 可以共享同一个寄存器。
  有边 = 必须用不同的寄存器。
]

== 图着色问题

#definition[
  图的 K-着色：给每个节点分配 K 种颜色之一，使得任意一条边的两端颜色不同。

  如果图可以用 K 种颜色着色，则所有变量可以用 K 个物理寄存器分配（无需溢出）。
]

```
K=2:
r0(红) — r1(蓝) — r2(红)   ← 可以！r0 和 r2 同色，它们之间无边
         r3(红)              ← r3 孤立，任意颜色都行
```

如果 `K=1`：r0—r1—r2 链不可能用 1 种颜色着色（相邻节点同色违规）。必须*溢出*某个节点。

#intuition[
  着色本身不是算法——它是判断*是否可行*的数学问题。

  真正的算法是：*当图不可着色时，选择哪个节点溢出，然后重新构建干涉图并重试着色*。

  K = 物理寄存器数量。K 越大，着色越容易。x86-64 有 16 个寄存器，通常够用；x86-32 只有 8 个，经常不够。
]

== Chaitin 算法

Chaitin 等人在 1981 年提出了第一个实用的图着色寄存器分配器。核心思想：*迭代着色 + 必要时溢出*。

=== 步骤

1. *构建*：分析代码，构建干涉图
2. *简化*：反复删除度数 < K 的节点，压入栈中
3. *溢出*：如果所有剩余节点度数都 >= K，选择一个节点标记为溢出，从图中删除
4. *选择*：从栈中弹出节点，尝试分配颜色（不与已着色的邻居冲突）
5. *重写*：如果有溢出节点，生成 spill code（load/store 指令），重新从步骤 1 开始

#story[
  为什么总是存在度数 < K 的节点可以被删除？

  这不是必然的——如果图中每个节点度数都 >= K，简化无法进行。
  这时就必须选择一个节点溢出。

  但*如果图中存在度数 < K 的节点*，删除它不会让图变得更难着色——
  因为它最多有 K-1 个邻居，染色时总有一个颜色可用。
]

=== 选择溢出候选

当所有节点度数 >= K 时，选择哪个溢出？

启发式策略：
- 选择*使用频率最低*的变量（spill cost 最小）
- 选择*定义和使用之间指令最少*的变量（活跃范围短）
- 选择不被频繁执行的循环中的变量

```
spill_cost(v) = Σ (10^loop_depth × (def_count + use_count))
```

循环内的变量溢出代价远高于循环外的——一个循环内变量被使用 100 次，溢出的代价就是 100 次额外的 load/store。

#derivation[
  为什么用 `10^loop_depth`？

  每层循环嵌套大约使执行次数增加 10 倍（粗略估计）。
  `loop_depth = 0`（不在循环中）→ 权重 1
  `loop_depth = 1`（一层循环内）→ 权重 10
  `loop_depth = 2`（嵌套循环内）→ 权重 100

  编译器会优先溢出循环外的不常用变量。
]

== Briggs 改进：乐观着色

Chaitin 算法在简化阶段就决定溢出（当所有节点度数 >= K 时）。但这个决策*过于悲观*。

```
; 反例：所有节点度数都 >= 2，但图是二分图，用 2 色可着色
a — b
|   |
c — d
```

Briggs 的改进：*推迟溢出决策*。即使在简化阶段所有节点度数 >= K，也不立即溢出，而是选择一个节点继续压栈。着色阶段*再检查*是否真的无法分配颜色。

优势：减少了不必要的溢出——有些图虽然简化阶段看起来不可着色，实际上可以。

== 合并 (Coalescing)

```
; 合并前的代码
r0 = a + b
r1 = r0     ; copy 指令
... use r1 ...
```

`r0` 和 `r1` 有 copy 关系——如果它们分配到同一个物理寄存器，`r1 = r0` 这条指令可以直接消除。

#definition[
  *合并*（coalescing）：如果两个节点之间有 copy 指令且它们不干涉，就把它们合并为一个节点。

  合并后，copy 指令被消除（因为两个"虚拟寄存器"现在是同一个物理寄存器）。
]

但合并可能让图变得更难着色——两个各自度数 < K 的节点合并后可能度数 >= K。因此 Chaitin 提出*保守合并*：只有当合并后的节点度数仍然 < K 时才合并。

#concept[
  图着色寄存器分配的完整 pipeline：

  ```
  代码 → 活跃分析 → 干涉图 → 合并(copy消除)
        → 简化(低度数节点压栈) → 溢出(选候选)
        → 选择(弹栈着色) → 溢出代码插入
        → 重新开始（如果有溢出）
  ```

  这个循环可能迭代多次——每次溢出都会改变干涉图。
]

== 和线性扫描的对比

| 方面 | 图着色 | 线性扫描 |
|:---|:---|:---|
| 时间复杂度 | 多项式（启发式） | O(n log n) |
| 分配质量 | 更优（全局视角） | 略差（约 5-10% 损失） |
| 实现复杂度 | 高 | 低 |
| 典型使用者 | GCC、LLVM（混合） | V8、HotSpot JIT |
| 溢出决策 | 全局 spill cost | 局部启发式 |
| 是否考虑干涉图结构 | 是 | 否 |

LLVM 从 3.0 开始使用*贪心寄存器分配器*（Greedy），它是图着色和线性扫描的混合体——用活跃区间做线性扫描，但在决策时参考干涉图的全局信息。

== 练习

#note[
  *题目位置*：`exercises/src/ch07_graph.rs`

  *任务*：实现图着色寄存器分配的核心步骤——从活跃区间构建干涉图。

  给你：`vec![a: [0,2], b: [1,3], c: [2,4], d: [0,5]]`

  你要构建一张图：节点是变量名，边连接所有活跃范围重叠的变量对。

  提示：两重循环判断每一对 `(i, j)`——如果 `i.end >= j.start && j.end >= i.start`，则 i 和 j 干涉。

  *验证*：`cd exercises && cargo test ch07`

  *答案*：`exercises/answers/ch07_graph.rs`
]

== 小结

- 干涉图：节点 = 虚拟寄存器，边 = 活跃范围重叠
- 着色：K 色分配给干涉图节点，相邻不同色
- Chaitin：构建 → 简化 → 溢出 → 选择 → 重写（循环迭代）
- Briggs：推迟溢出决策，减少不必要的 spill
- 合并：消除 copy 指令——前提是不增加着色难度
- 溢出候选选择：spill_cost = Σ(10^loop_depth × use_count)
- 图着色质量更好但更慢；线性扫描更快但稍差
- LLVM Greedy = 两者的混合
#pagebreak()
