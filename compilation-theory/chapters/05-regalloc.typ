#import "../lib.typ": *
= 寄存器分配 — 编译器的心脏
#labnote[ 第五站：有限资源的优雅分配 ]

在三地址码中，我们假设有无穷多个虚拟寄存器。但真实的 CPU 只有十几个通用寄存器。

*寄存器分配*就是把无限虚拟寄存器映射到有限物理寄存器的过程。这是编译器中公认最难、也最重要的优化步骤之一。

== 问题的核心

```
; 三地址码（无限虚拟寄存器）
r0 = a + b
r1 = r0 * c
r2 = d - e
r3 = r1 + r2
return r3
```

只有 4 个物理寄存器怎么办？如果虚拟寄存器超过物理寄存器数量，就必须把一些值*溢出*（spill）到内存（栈）中，需要时再 load 回来。

```
; 假设只有 2 个寄存器: rA, rB
load rA, [a]
load rB, [b]
add rA, rB           ; rA = r0 = a + b
store [r0_tmp], rA    ; 溢出 r0 到栈
load rA, [c]
mul rA, [r0_tmp]     ; rA = r1 = r0 * c
store [r1_tmp], rA    ; 溢出 r1
load rA, [d]
load rB, [e]
sub rA, rB            ; rA = r2 = d - e
load rB, [r1_tmp]    ; 恢复 r1
add rA, rB            ; rA = r3 = r1 + r2
```

因为寄存器不够，额外产生了 4 条 load/store 指令——纯开销。

== 活跃范围 (Live Range)

#definition[
  一个虚拟寄存器的*活跃范围*是从它被定义（写入）到最后一次被使用之间的所有指令。

  ```
  r0 = a + b          ← r0 定义
  r1 = r0 * c         ← r0 最后使用（之后 r0 dead）
  r2 = d - e
  r3 = r1 + r2
  ```

  r0 的活跃范围：从第 1 行到第 2 行。
  r1 的活跃范围：从第 2 行到第 4 行。
  r2 的活跃范围：从第 3 行到第 4 行。
]

两个活跃范围不重叠的虚拟寄存器可以共享同一个物理寄存器。

#intuition[
  *寄存器分配的核心思想*：

  如果两个变量永远不会"同时存活"，它们就可以复用同一个物理寄存器。

  就像停车位——如果两辆车从不同时在场，它们可以共用一个车位。
]

== 干涉图

把活跃范围转化为图：每个变量是一个节点，两个重叠的变量之间有一条边。无边的节点可以共享寄存器。

#concept[
  这就是*干涉图*（interference graph）——下一章会详细展开图着色的完整算法（Chaitin-Briggs）。

  这里需要理解的核心：寄存器分配 = 干涉图 + K 色着色 + 必要时溢出。
]

== 线性扫描分配

最简单的分配算法——按指令顺序扫描，维护一个活跃列表：

+ 遇到新的虚拟寄存器 → 分配一个空闲物理寄存器
+ 如果物理寄存器用完 → 选择活跃范围最远的溢出
+ 虚拟寄存器死亡 → 释放物理寄存器

线性扫描比图着色快得多（O(n) vs NP），生成的代码稍差（约 5-10% 性能损失），但在 JIT 编译器（如 V8）中广泛使用。

== 这和 MLOG 有什么关系

MLOG 没有物理寄存器限制。每个 MLOG 变量相当于一个虚拟寄存器——处理器内部管理它们的存储。

#intuition[
  *MLOG 编译器不需要寄存器分配*——这是它和 x86 编译器最大的架构差异。

  这也是为什么 MLOG 编译器只有 1000 行——它绕过了编译器中最难的一个问题。

  但理解寄存器分配仍然是必要的：它让你理解 x86/Rust 编译器在做什么，
  以及为什么 MLOG 编译器的设计可以如此简洁。
]

== 练习

#note[
  *题目位置*：`exercises/src/ch06_regalloc.rs`

  *任务*：实现 `linear_scan` 函数，用线性扫描算法把活跃区间映射到 K 个物理寄存器。

  给你：`[a: [0,5], b: [1,4], c: [2,3]]`，2 个物理寄存器

  你要返回：`a→reg0, b→reg1, c→None`（c 溢出——它和 a/b 都重叠，但寿命最短）

  提示：按 start 排序后遍历。维护 `active`（已分配区间+寄存器）和 `free_regs`。寄存器用完时，找到 active 中 end 最大的那个——溢出 end 较小的那个（寿命更短，spill 代价更小）。

  *验证*：`cd exercises && cargo test ch06`

  *答案*：`exercises/answers/ch06_regalloc.rs`
]

== 小结

- 寄存器分配 = 无限虚拟 → 有限物理的映射
- 活跃范围决定哪些变量可以共享寄存器
- 干涉图是图着色法的核心数据结构（下一章深入）
- 线性扫描 = 更快、稍差——O(n) 复杂度
- MLOG 不需要寄存器分配——变量数量无限制
#pagebreak()
