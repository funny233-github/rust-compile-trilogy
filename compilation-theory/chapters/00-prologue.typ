#import "../lib.typ": *
= 序章：代码生成的普遍性

编译器是什么？答案很简单，但展开之后极其宏大：

> 编译器是一个*翻译器*——把一种语言的程序转换为另一种语言的等价程序。

每一层翻译丢掉一些抽象，添加一些细节，直到目标机能直接执行。

== 三条翻译链

把三条翻译链并排放在一起看：

#table(
  columns: (auto, auto, auto, auto),
  fill: (rgb("#e5e7eb"),),
  inset: 6pt,
  stroke: 0.5pt,
  [*层次*], [*C → x86*], [*Rust → LLVM*], [*MLOG DSL → MLOG*],
  [1. 源语言], [C 源码], [Rust 源码], [MLOG DSL],
  [2. 中间层], [GIMPLE / RTL], [MIR → LLVM IR], [AST → 三地址码 IR],
  [3. 后端], [x86 汇编], [x86 / ARM / WASM], [MLOG 指令],
  [4. 最终], [机器码], [机器码], [字符串文本],
)

#intuition[
  三条链的共同骨架：

  ```
  源语言 → 中间表示 (IR) → 目标语言
  ```

  中间的 IR 是编译器设计的*支点*。所有的分析、优化、变换都在 IR 上发生。
  前端的差异被 IR 吸收，后端的差异从 IR 出发。
]

== 每一层丢掉什么、添加什么

#table(
  columns: (auto, auto, auto),
  fill: (rgb("#e5e7eb"),),
  inset: 6pt,
  stroke: 0.5pt,
  [*翻译步骤*], [*丢掉*], [*添加*],
  [C `for` → 汇编], [循环语义的「为什么」], [标签名、cmp 指令、jmp 指令],
  [`x = a + b * c` → IR], [运算符优先级], [临时变量 `__t0`、指令顺序],
  [Rust `match` → LLVM], [模式匹配的穷尽性], [跳转表/分支链],
  [MLOG DSL `while` → MLOG], [块作用域], [标签、条件跳转],
)

编译器每往下走一层，就更接近机器的思维，更远离人的思维。

== 核心问题

本书围绕一个核心问题展开：

> 高级语言中的构造（变量、表达式、循环、函数、类型）在低层如何表示？

不局限于某一种语言或某一种目标——我们关注的是变换本身的*通用模式*。

== 本书结构

*第一章*：从 CPU 的视角看世界——它只能做什么
*第二至四章*：C → 汇编的经典三部曲——表达式、控制流、函数
*第五章*：寄存器分配——编译器的心脏
*第六章*：三地址码——编译器的通用语言
*第七章*：从 C 到 Rust——多了什么，少了什么
*第八章*：Rust 特有构造的编译——enum、trait、闭包
*第九章*：优化——编译器在帮你做什么
*第十章*：回到 MLOG——理论如何指导实践
*第十一章*：到过程宏——编译时翻译的通用框架
*第十二章*：翻译链的普遍性

每一章都从具体代码出发，展示它如何被一层层翻译，直到机器能理解的形式。

开始吧。
#pagebreak()
